🔔 Reminder: Run the following commands before working in this container:

```
gcloud auth login
gcloud auth application-default set-quota-project flarum-oss-forum
gcloud config set project flarum-oss-forum
```
